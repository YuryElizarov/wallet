export type Colors = {
  body: string;
  background: string;
  background2: string;
  content: string;
  contentOpacity: string;
  red: string;
  green: string;
  blue: string;
  input: string;
  border: string;
  placeholder: string;
};
export type ColorsKey = keyof Colors;
